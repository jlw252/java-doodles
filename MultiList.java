package jennyworld;

public class MultiList {
		     public int type;
		     public int value;
		     public MultiList car;
		     public MultiList cdr;
		     public MultiList() {type = 0; value = 0; car = null; cdr = null;}
	
		  
	//public static MultiList readCdr(){}
	//public static MultiList readCar(){}
	/*
	 * read Car: 
	 * token = input.next();
	 * create node
	 * if token is an integer
	 * else if token is #t
	 * else if token is #f
	 * else if token is )
	 * else if token is (
	 * create L node
	 * node.Car = readCar();
	 * node.cdr = readCdr();
	 * return node;
	 * not read in Ctr
	 */
	
	//public class MultiList{}

}
